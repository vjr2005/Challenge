import SnapshotTesting
import SwiftUI
import Testing

@testable import ChallengeDesignSystem

/*
@Suite(.timeLimit(.minutes(1)))
struct DSBadgeSnapshotTests {
	init() {
		UIView.setAnimationsEnabled(false)
	}

	// MARK: - Individual Variants

	@Test
	func successBadge() {
		let view = DSBadge("Success", variant: .success)
			.padding()

		assertSnapshot(of: view, as: .image)
	}

	@Test
	func errorBadge() {
		let view = DSBadge("Error", variant: .error)
			.padding()

		assertSnapshot(of: view, as: .image)
	}

	@Test
	func warningBadge() {
		let view = DSBadge("Warning", variant: .warning)
			.padding()

		assertSnapshot(of: view, as: .image)
	}

	@Test
	func neutralBadge() {
		let view = DSBadge("Neutral", variant: .neutral)
			.padding()

		assertSnapshot(of: view, as: .image)
	}

	@Test
	func accentBadge() {
		let view = DSBadge("Accent", variant: .accent)
			.padding()

		assertSnapshot(of: view, as: .image)
	}

	// MARK: - All Variants Gallery

	@Test
	func allVariantsGallery() {
		let view = VStack(spacing: SpacingToken.md) {
			DSBadge("Success", variant: .success)
			DSBadge("Error", variant: .error)
			DSBadge("Warning", variant: .warning)
			DSBadge("Neutral", variant: .neutral)
			DSBadge("Accent", variant: .accent)
		}
		.padding()

		assertSnapshot(of: view, as: .image)
	}
}
*/
