import ProjectDescription
import ProjectDescriptionHelpers

let workspace = Workspace(
	name: appName,
	projects: [
		".",
		coreModule.path,
		networkingModule.path,
		snapshotTestKitModule.path,
		designSystemModule.path,
		resourcesModule.path,
		characterModule.path,
		episodeModule.path,
		homeModule.path,
		systemModule.path,
		appKitModule.path,
	],
	schemes: AppScheme.allSchemes() + [AppScheme.uiTestsScheme()],
	generationOptions: .options(
		autogeneratedWorkspaceSchemes: .enabled(
			codeCoverageMode: .targets(Modules.codeCoverageTargets),
			testingOptions: []
		)
	)
)
